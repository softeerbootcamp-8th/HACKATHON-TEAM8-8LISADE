@AGENTS.md

## Claude Code

8LISADE Skill 원본은 `.claude/skills/`에 있다(Claude Code 프로젝트 자동 인식 경로). Codex 등 다른 Agent 도구가 같은 Skill을 읽을 수 있도록 `.agents/skills/`에 동일 내용의 symlink를 함께 두었다 — 수정할 때는 `.claude/skills/` 쪽 원본만 고치면 된다.
