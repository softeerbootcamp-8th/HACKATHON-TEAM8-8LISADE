server {
    listen 80;
    listen [::]:80;
    server_name api.8lisade.site;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
