# 출석 미션 PIN 직접 진입 (#192)

학생이 출석(CHECK) 미션의 `현재 미션 수행`을 선택하면 `CheckMissionScreen`이 바로
4자리 PIN 입력 폼을 보여준다. 기존의 중간 `출석 체크` 버튼과 `open` 상태는 제거했다.

`App.test.tsx`는 PIN 입력 즉시 노출, 중간 버튼 미노출, 잘못된 PIN 오류, 정상 완료 후
다음 미션 진행 및 뒤로 가기 흐름을 검증한다. 사진(ACTIVITY) 미션의 촬영·제출 흐름은
변경하지 않았다.
