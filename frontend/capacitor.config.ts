import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.softeerbootcamp.eightlisade',
  appName: '8LISADE',
  webDir: 'dist',
  loggingBehavior: 'debug',
}

export default config
